import React from 'react';
import './Note.css'

function Note() {

    return (
        <div className='Note'>
        <p>*Note that this is a static version of the app so at this time the pattern cannot be altered and the VOLUME and BPM values are fixed. Push play to hear a placeholder audio track of the displayed pattern. Full interactive functionality will be added in the next iteration of the app.</p>
        </div>
    )
}

export default Note;
